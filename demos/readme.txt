This demos shows the usage of the DataRowConverter and DataSet in different GUI-Toolkits (and it is all the same).

= WPF =
PersonDemo(Toolkit.Mvvm)
PersonDemo(Unity)

= Windows Forms =
PersonDemo(WinForms)
- u must build the project before open the MainForm Designer

= Web =
PersonDemo(Web)
- very simple build
